// Uso de los bucles

for (int i = 0; i < 10; i++)
{
    Console.WriteLine(i);
}

// Recorrer un array con foreach
var myArray= new String[] {"Yukiel","Kevin","Codere"};
foreach (var m in myArray)
{
    Console.WriteLine(m);
}